{
  "author": "Rijnhard Hessel <r.hessel.git@gmail.com>",
  "name": "ini2",
  "description": "An ini encoder/decoder for node based on Isaacs ini but with support for decoding filters",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/rijnhard/ini.git"
  },
  "main": "ini.js",
  "scripts": {
    "pretest": "standard ini.js",
    "test": "tap test/*.js --100 -J",
    "preversion": "npm test",
    "postversion": "npm publish",
    "postpublish": "git push origin --all; git push origin --tags"
  },
  "engines": {
    "node": "*"
  },
  "dependencies": {},
  "devDependencies": {
    "standard": "^10.0.3",
    "tap": "^10.7.3 || 11"
  },
  "license": "ISC",
  "files": [
    "ini.js"
  ]
}
